{
  "name": "layanan-aspirasi",
  "version": "2.0.0",
  "description": "Layanan aspirasi berbasis web. Cloudflare Pages + Neon Postgres + notifikasi Telegram.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.js",
    "uji": "node uji.js",
    "dev:cf": "npx wrangler pages dev",
    "deploy": "npx wrangler pages deploy"
  },
  "//": "Hanya satu dependency, disengaja. Wrangler TIDAK didaftarkan di sini karena menarik puluhan paket native (workerd, sharp) yang memperlambat build Cloudflare dan tidak dibutuhkan: deploy berjalan lewat integrasi Git. Skrip dev:cf dan deploy memakai npx, jadi wrangler diunduh hanya saat benar-benar dipanggil.",
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
